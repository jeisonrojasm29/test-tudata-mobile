import { StyleSheet } from "react-native";
import { colors, vh } from "../../../utils/globalStyles";

export const styles = StyleSheet.create({
  checkbox: {
    width: vh(1),
    height: vh(1),
    borderWidth: 1,
    borderColor: colors.color3
  }
})